
function [spam,nospam] = fNaiveBayesian(email_spam,email_nonspam, text_probabilities);



for i =1:length(email)
    
    if email(i,2) ~= 0
        spam = spam * email(i,3);
        no_spam = no_spam * email(i,2);
        
    end
    
end


end 