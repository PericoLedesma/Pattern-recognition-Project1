% Functions

clear all
clc


% Array definition
% array = importdata('task_1.mat');
% size_array = size(array);


array = [1 1;3 0;-1 -1];
array = [1 1;-1 -1]


size_array = size(array);

number_rows = size_array(1, 1);
number_columns = size_array(1, 2);


% Calculation

C = cov(array)

%cov = E(xy) - E(X)E(y) --> E = mean
%var = E(X*X) - E(X)^2

i = 1;
j = 1;

column1 = array(:,i);
column2 = array(:,j);

a = column1.* column2

dot_average = mean(a)

avg1 = mean (column1);
avg2 = mean (column2);

solution1 = dot_average - avg1 * avg2;

% Opcion 2

i = 1;
j = 1;

column1 = array(:,i);
column2 = array(:,j);


x = 0; 
convariance = zeros (number_columns, number_columns)

for j = 1:number_columns
    
for i = 1:number_rows
    c = (column1(i) - avg1) * (column2(i) - avg2) + c;
end

end

solution = c /(number_rows - 1)